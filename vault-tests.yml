- name: play1
  hosts: localhost
  gather_facts: false
  vars:
    - var1: "test"
  vars_files:
    - vault_secrets.yml
  tasks:
    - name: "Print variables"
      debug:
        msg:
          - "{{ item }}: {{ query('vars', item)|first }}"
      loop: "{{ query('varnames', '^vault.+$') }}" 
